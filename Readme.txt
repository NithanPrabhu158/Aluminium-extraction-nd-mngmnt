# Aluminium Extraction and Management System 
It will predict the amount of aluminium extracted and red mud produced using the reinforcement Learning
# step 1. Install the packages
pip install -r requirements.txt
# step 2. Go to the Project directory
 cd code
 cd main_aluminium   
 # step 3. Run the server
python manage.py runserver  
# step 4. click the url to run the project in the browser

